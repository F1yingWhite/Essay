切片数据如下:
```bash
python deepzoom_tiler.py -m 0 -b 20 -d LBM_Path #这里的thres是30
python deepzoom_tiler.py -m 0 -b 20 -d SVS -v ndpi #这里的thres是15
python deepzoom_tiler.py -m 0 -b 20 -d SVS -v mrxs #这里的thres是15
```
